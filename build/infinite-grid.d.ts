export declare function drawInfiniteGrid(canvas: HTMLCanvasElement, ctx: CanvasRenderingContext2D, options: {
    offset: {
        x: number;
        y: number;
    };
    scale: number;
    backgroundColor: string;
    gridColor: string;
}): void;
