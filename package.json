{
  "name": "html-canvas-utilities",
  "private": true,
  "version": "0.0.0",
  "main": "build/index.js",
  "module": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "dev": "vite --host",
    "package": "rm -rf build && tsc -p tsconfig.module.json",
    "postinstall": "npm run package",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "lit": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "rollup-plugin-typescript2": "^0.31.2",
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
